export default function Footer() {
  return (
    /* version info */
    <footer className="mt-8 pb-2 text-center text-[11px] font-bold text-text-muted">
        Portfolio <b>v2.2</b> — Now responsive across devices!
    </footer>
  )
}
