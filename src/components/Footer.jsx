export default function Footer() {
  return (
    /* version info */
    <footer className="mt-4 text-center text-[11px] font-bold text-text-muted">
        Portfolio <b>v2.1</b> — Now with responsive buttons and links
    </footer>
  )
}
